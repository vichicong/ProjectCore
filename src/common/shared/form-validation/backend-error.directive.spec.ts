import { BackendErrorDirective } from './backend-error.directive'

describe('BackendErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new BackendErrorDirective()
    expect(directive).toBeTruthy()
  })

})
